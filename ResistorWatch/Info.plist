<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<!-- Silent CloudKit push wake-up, NOT a user-facing notification. SwiftData's
	     CloudKit sync (NSPersistentCloudKitContainer) learns the remote database
	     changed only via a silent push; without this the store imports on launch
	     and whenever its engine next runs. No UNUserNotificationCenter
	     authorization, no prompt, no alert/badge/sound. -->
	<!-- The watch requests its own when-in-use authorization; the phone's grant
	     does not carry over. Without this key CoreLocation refuses the request
	     outright and every wrist log is saved without a location. -->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>Needed to track resistance locations.</string>
	<key>UIBackgroundModes</key>
	<array>
		<string>remote-notification</string>
	</array>
	<key>WKApplication</key>
	<true/>
	<key>WKCompanionAppBundleIdentifier</key>
	<string>com.resistor.app</string>
</dict>
</plist>
